name: recycle_fashion
description: Recycle old clothes → earn vouchers for new ones.
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.8
  shared_preferences: ^2.3.2
  # (clipboard is part of flutter/material, no extra dep needed)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
